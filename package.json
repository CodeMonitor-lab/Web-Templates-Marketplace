{
  "name": "@root/marketplace",
  "version": "1.0.0",
  "private": true,

  "packageManager": "pnpm@10.25.0",

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "turbo format",
    "clean": "turbo clean"
  },

  "devDependencies": {
    "turbo": "^2.9.14"
  }
}